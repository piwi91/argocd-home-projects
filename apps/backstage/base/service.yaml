---
apiVersion: v1
kind: Service
metadata:
  name: backstage
  labels:
    app: backstage
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: backstage
  ports:
    - name: http
      port: 80
      targetPort: 7007
      protocol: TCP